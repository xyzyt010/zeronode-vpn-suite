#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
        systemctl stop zeronode-vpn-server.service || true
        systemctl disable zeronode-vpn-server.service || true
    fi
fi

exit 0
